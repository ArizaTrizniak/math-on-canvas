# 404 Not Found Page — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a branded, localized 404 page that matches the Math on Canvas landing page visual identity.

**Architecture:** A single async Server Component at `src/app/not-found.tsx` detects the user's language via header chain (URL path → Accept-Language → English), imports the corresponding translation JSON directly (same pattern as `LandingPage.tsx`), and renders a full-viewport page with header, centered 404 section, and footer.

**Tech Stack:** Next.js 16 App Router, TypeScript, CSS modules (plain CSS files), `next/headers` for server-side header access.

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `src/lib/i18n/locales/en/not-found.json` | EN translations |
| Create | `src/lib/i18n/locales/ru/not-found.json` | RU translations |
| Create | `src/lib/i18n/locales/es/not-found.json` | ES translations |
| Create | `src/lib/i18n/locales/de/not-found.json` | DE translations |
| Create | `src/app/NotFoundPage.css` | Page styles |
| Create | `src/app/not-found.tsx` | 404 page component |
| Modify | `.gitignore` | Add `.superpowers/` |

---

## Task 1: Add `.superpowers/` to `.gitignore`

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: Add the entry**

Open `.gitignore` and add after the `/.worktrees` line:

```
/.worktrees
/.superpowers
```

- [ ] **Step 2: Commit**

```bash
git add .gitignore
git commit -m "chore: ignore .superpowers brainstorm directory"
```

---

## Task 2: Create translation JSON files

**Files:**
- Create: `src/lib/i18n/locales/en/not-found.json`
- Create: `src/lib/i18n/locales/ru/not-found.json`
- Create: `src/lib/i18n/locales/es/not-found.json`
- Create: `src/lib/i18n/locales/de/not-found.json`

- [ ] **Step 1: Create English translations**

`src/lib/i18n/locales/en/not-found.json`:
```json
{
    "heading": "Page Not Found",
    "description": "This page has been moved or no longer exists. Try going back to the home page or opening the editor.",
    "goHome": "Back to Home",
    "openEditor": "Open Editor",
    "footer": "© 2025 Math on Canvas"
}
```

- [ ] **Step 2: Create Russian translations**

`src/lib/i18n/locales/ru/not-found.json`:
```json
{
    "heading": "Страница не найдена",
    "description": "Эта страница была перемещена или больше не существует. Попробуй вернуться на главную или открыть редактор.",
    "goHome": "На главную",
    "openEditor": "Открыть редактор",
    "footer": "© 2025 Math on Canvas"
}
```

- [ ] **Step 3: Create Spanish translations**

`src/lib/i18n/locales/es/not-found.json`:
```json
{
    "heading": "Página no encontrada",
    "description": "Esta página fue movida o ya no existe. Intenta volver a la página principal o abrir el editor.",
    "goHome": "Ir al inicio",
    "openEditor": "Abrir editor",
    "footer": "© 2025 Math on Canvas"
}
```

- [ ] **Step 4: Create German translations**

`src/lib/i18n/locales/de/not-found.json`:
```json
{
    "heading": "Seite nicht gefunden",
    "description": "Diese Seite wurde verschoben oder existiert nicht mehr. Versuche, zur Startseite zurückzukehren oder den Editor zu öffnen.",
    "goHome": "Zur Startseite",
    "openEditor": "Editor öffnen",
    "footer": "© 2025 Math on Canvas"
}
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/i18n/locales/en/not-found.json \
        src/lib/i18n/locales/ru/not-found.json \
        src/lib/i18n/locales/es/not-found.json \
        src/lib/i18n/locales/de/not-found.json
git commit -m "feat: add not-found translations for en/ru/es/de"
```

---

## Task 3: Create `NotFoundPage.css`

**Files:**
- Create: `src/app/NotFoundPage.css`

- [ ] **Step 1: Write the stylesheet**

`src/app/NotFoundPage.css`:
```css
.notfound {
    min-height: 100vh;
    padding: 28px 32px 48px;
    background: linear-gradient(180deg, #f8fafc 0%, #f3f4f6 45%, #f8fafc 100%);
    color: #0f172a;
    display: flex;
    flex-direction: column;
    position: relative;
}

.notfound__texture {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
}

.notfound__grid {
    position: absolute;
    inset: 0;
    background-image:
        linear-gradient(rgba(17, 36, 48, 0.08) 1px, transparent 1px),
        linear-gradient(90deg, rgba(18, 33, 44, 0.2) 1px, transparent 1px);
    background-size: 36px 36px, 36px 36px;
    mask-image: radial-gradient(circle at 40% 25%, black 0%, transparent 70%);
}

.notfound__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    position: relative;
    z-index: 1;
}

.notfound__brand {
    display: flex;
    align-items: center;
    gap: 12px;
}

.notfound__brand-logo {
    width: 36px;
    height: 36px;
    border-radius: 10px;
    object-fit: contain;
    background: white;
    padding: 5px;
    box-shadow: 0 6px 18px rgba(15, 23, 42, 0.08);
}

.notfound__brand-title {
    font-weight: 700;
    font-size: 16px;
    line-height: 1.2;
    display: flex;
    align-items: center;
    gap: 8px;
}

.notfound__beta-badge {
    background: rgba(234, 179, 8, 0.15);
    color: #ca8a04;
    font-size: 0.6rem;
    font-weight: 700;
    padding: 2px 6px;
    border-radius: 999px;
    letter-spacing: 0.05em;
    border: 1px solid rgba(234, 179, 8, 0.3);
    vertical-align: middle;
}

.notfound__brand-subtitle {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    color: #4c5c67;
}

.notfound__nav-link {
    padding: 9px 16px;
    border-radius: 12px;
    border: 1px solid #111827;
    background: transparent;
    color: #111827;
    font-weight: 600;
    font-size: 14px;
    transition: background 120ms ease, color 120ms ease;
}

.notfound__nav-link:hover {
    background: #111827;
    color: white;
}

.notfound__main {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 20px;
    text-align: center;
    position: relative;
    z-index: 1;
    padding: 48px 24px;
}

.notfound__code {
    font-size: clamp(80px, 14vw, 120px);
    font-weight: 900;
    line-height: 1;
    color: #0f172a;
    letter-spacing: -4px;
}

.notfound__title {
    font-size: clamp(20px, 3vw, 28px);
    font-weight: 700;
    color: #111827;
    margin: 0;
}

.notfound__description {
    font-size: 15px;
    color: #4b5563;
    max-width: 400px;
    line-height: 1.6;
    margin: 0;
}

.notfound__actions {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 8px;
}

.notfound__ghost {
    padding: 11px 20px;
    border-radius: 12px;
    border: 1px solid #111827;
    background: transparent;
    color: #111827;
    font-weight: 600;
    font-size: 14px;
    transition: background 120ms ease, color 120ms ease;
}

.notfound__ghost:hover {
    background: #111827;
    color: white;
}

.notfound__cta {
    padding: 11px 20px;
    border-radius: 12px;
    border: 1px solid rgba(232, 146, 46, 0.6);
    background: linear-gradient(135deg, #ffbe7a, #e86f1d);
    color: #2c1604;
    font-weight: 800;
    font-size: 14px;
    box-shadow: 0 10px 32px rgba(232, 146, 46, 0.3);
    transition: transform 160ms ease, box-shadow 160ms ease;
}

.notfound__cta:hover {
    transform: translateY(-1px);
    box-shadow: 0 14px 36px rgba(15, 23, 42, 0.28);
}

.notfound__footer {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 8px;
    color: #6b7280;
    font-size: 12px;
    position: relative;
    z-index: 1;
}

.notfound__version {
    opacity: 0.5;
}

@media (max-width: 720px) {
    .notfound {
        padding: 20px 16px 32px;
    }

    .notfound__header {
        flex-direction: column;
        align-items: flex-start;
    }

    .notfound__nav-link {
        align-self: flex-end;
    }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/NotFoundPage.css
git commit -m "feat: add NotFoundPage styles"
```

---

## Task 4: Create `not-found.tsx`

**Files:**
- Create: `src/app/not-found.tsx`

- [ ] **Step 1: Write the component**

`src/app/not-found.tsx`:
```tsx
import Image from 'next/image'
import Link from 'next/link'
import { headers } from 'next/headers'
import type { Metadata } from 'next'
import { LANGUAGES, normalizeLanguage, type LanguageCode } from '@/lib/i18n/constants'
import notFoundEN from '@/lib/i18n/locales/en/not-found.json'
import notFoundRU from '@/lib/i18n/locales/ru/not-found.json'
import notFoundES from '@/lib/i18n/locales/es/not-found.json'
import notFoundDE from '@/lib/i18n/locales/de/not-found.json'
import './NotFoundPage.css'

export const metadata: Metadata = {
    title: '404 — Math on Canvas',
    robots: { index: false, follow: false },
}

const translations = {
    en: notFoundEN,
    ru: notFoundRU,
    es: notFoundES,
    de: notFoundDE,
} as const

async function detectLang(): Promise<LanguageCode> {
    const headersList = await headers()

    const nextUrl = headersList.get('next-url') ?? ''
    const firstSegment = nextUrl.split('/').filter(Boolean)[0] ?? ''
    const langFromUrl = LANGUAGES.find(l => l.code === firstSegment)?.code
    if (langFromUrl) return langFromUrl

    const acceptLang = headersList.get('accept-language') ?? ''
    if (acceptLang) {
        const primary = acceptLang.split(',')[0]?.split(';')[0]?.trim() ?? ''
        return normalizeLanguage(primary)
    }

    return 'en'
}

export default async function NotFound() {
    const lang = await detectLang()
    const t = translations[lang]

    return (
        <div className="notfound">
            <div className="notfound__texture" aria-hidden="true">
                <span className="notfound__grid" />
            </div>

            <header className="notfound__header">
                <div className="notfound__brand">
                    <Image
                        className="notfound__brand-logo"
                        src="/images/logo.svg"
                        alt="Math on Canvas"
                        width={36}
                        height={36}
                        unoptimized
                    />
                    <div>
                        <div className="notfound__brand-title">
                            Math on Canvas
                            <span className="notfound__beta-badge">BETA</span>
                        </div>
                        <div className="notfound__brand-subtitle">mathoncanvas.com</div>
                    </div>
                </div>
                <Link href={`/${lang}`} className="notfound__nav-link">
                    ← {t.goHome}
                </Link>
            </header>

            <main className="notfound__main">
                <div className="notfound__code" aria-hidden="true">404</div>
                <h1 className="notfound__title">{t.heading}</h1>
                <p className="notfound__description">{t.description}</p>
                <div className="notfound__actions">
                    <Link href={`/${lang}`} className="notfound__ghost">
                        ← {t.goHome}
                    </Link>
                    <Link href="/editor" className="notfound__cta">
                        {t.openEditor} →
                    </Link>
                </div>
            </main>

            <footer className="notfound__footer">
                {t.footer}
                <span className="notfound__version">
                    v{process.env.NEXT_PUBLIC_APP_VERSION}
                </span>
            </footer>
        </div>
    )
}
```

- [ ] **Step 2: Run TypeScript check**

```bash
npx tsc --noEmit
```

Expected: no errors. If you see errors about missing JSON modules — add `"resolveJsonModule": true` to `tsconfig.json` (it's almost certainly already there in a Next.js project).

- [ ] **Step 3: Run dev server and manually verify**

```bash
npm run dev
```

Open the following URLs and verify each one:

| URL | Expected behaviour |
|-----|--------------------|
| `http://localhost:3000/nonexistent` | 404 page in English (URL has no lang segment) |
| `http://localhost:3000/ru/nonexistent` | 404 page in Russian (lang from URL path) |
| `http://localhost:3000/fr/nonexistent` | 404 page — `fr` is invalid, detected from Accept-Language or defaults to English |
| `http://localhost:3000/fr` | 404 page — `notFound()` triggered by invalid lang code |

Checklist for each URL:
- [ ] HTTP status 404 (check DevTools → Network tab, look for the document request)
- [ ] Brand header visible (logo + "Math on Canvas" + BETA badge)
- [ ] "← Back to Home" link in header points to `/{lang}` (e.g. `/en` or `/ru`)
- [ ] Large "404" displayed in center
- [ ] Localized `<h1>` text
- [ ] Two CTA buttons: ghost + orange gradient
- [ ] Orange button links to `/editor`
- [ ] Footer shows copyright + version

- [ ] **Step 4: Commit**

```bash
git add src/app/not-found.tsx
git commit -m "feat: add branded localized 404 not-found page"
```

---

## Task 5: Final cleanup and production build check

- [ ] **Step 1: Run production build**

```bash
npm run build
```

Expected: build completes without errors. The `not-found` route will appear in the output as a static page with 404 status.

- [ ] **Step 2: Verify `noindex` in rendered HTML**

Start the production server:
```bash
npm run start
```

Open `http://localhost:3000/nonexistent` and view page source. Verify the `<head>` contains:
```html
<meta name="robots" content="noindex,nofollow"/>
```

- [ ] **Step 3: Stop server and push branch**

```bash
git log --oneline -5
```

Verify the 3 feature commits are present:
1. `feat: add not-found translations for en/ru/es/de`
2. `feat: add NotFoundPage styles`
3. `feat: add branded localized 404 not-found page`