User-agent: *
Allow: /

# The editor is an app surface, not a document — nothing there to index.
Disallow: /editor
Disallow: /editor/
# Auth callbacks and API routes return no indexable HTML.
Disallow: /api/
Disallow: /auth/
Disallow: /admin/

# Host must match the canonical origin: the bare domain 307-redirects to www,
# so declaring the bare host here would point crawlers through a redirect.
Sitemap: https://www.math-on-canvas.com/sitemap.xml
